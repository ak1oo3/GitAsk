# --- Ingestion / chunking (Milestone 1) ---
tree-sitter>=0.21,<0.22
tree-sitter-languages>=1.10.2
GitPython>=3.1.43

# --- Embeddings (Milestone 2) ---
sentence-transformers>=3.0.0      # bge-small/base local embeddings (default, no API key)
openai>=1.40.0                    # optional: text-embedding-3-small

# --- Vector store: pgvector (Milestone 2) ---
psycopg[binary]>=3.2.0
pgvector>=0.3.0

# --- Hybrid retrieval / BM25 re-ranking (Milestone 3) ---
rank-bm25>=0.2.2

# --- LLM answer synthesis (Milestone 3/4) ---
anthropic>=0.34.0

# --- Backend API (Milestone 4) ---
fastapi>=0.114.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1
jinja2>=3.1.4

# --- Eval (Milestone 5) ---
numpy>=1.26.0
tabulate>=0.9.0

# --- Dev / test ---
pytest>=8.3.0
